# Cloudflare Pages 配置：Pages Functions 项目必需的 wrangler.toml
# 让 wrangler pages deploy 正确识别此项目，并指定部署的静态产物目录
name = "eldercare-charity"
compatibility_date = "2026-08-19"

# pages_build_output_dir 必须放在 [build] 段内，wrangler 才能识别产物目录
[build]
pages_build_output_dir = "dist"